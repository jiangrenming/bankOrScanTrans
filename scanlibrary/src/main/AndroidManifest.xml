<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.nld.starpos.wxtrade">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".activity.StartScanActivity"/>
        <activity android:name=".activity.MessageTipActivity"/>
    </application>

</manifest>
